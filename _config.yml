# Site settings
title: auranbuckles
email: a@auranbuckles.com
description: > # this means to ignore newlines until "baseurl:"
baseurl: "" # the subpath of your site, e.g. /blog/
url: "" # the base hostname & protocol for your site

linkedin_username: auranbuckles
github_username: auranbuckles

# Build settings
markdown: kramdown

exclude:
  - "bower_components"
  - "bower.json"

# require jekyll-sitemap after other gems if want sitemap to include generated content
plugins: [jekyll-paginate, jekyll-feed, jekyll-compose, jekyll-sitemap]
